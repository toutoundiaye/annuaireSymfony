{% block body %}
    <div class="jumbotron">
        <div class="container">
            <h1>Ajouter un nouveau contact</h1>

            {# Début formulaire (ajout ou modification) #}
			    {{ form_start(form) }}
                {# Champ du formulaire #}
			   {{ form(form) }}
            {# Fin formulaire #}
            {{ form_end(form) }}
        </div>
    </div>
    {% endblock %}